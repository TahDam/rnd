//
// Created by alexandr_v_zaharchuk on 07.02.16.
//

#include <string.h>
#include <printf.h>
#include <stdio.h>

const char *flrnm(const char *fln) {
    char flnMSS [strlen(fln)];
    char * lstchar;

    /*
    for (int i = 0; i < strlen(flnMSS); ++i) {
        // lstchar = flnMSS[i];


    }
     */

    int tmp = sizeof(flnMSS);
    printf("Size of flnMSS = %i", tmp);

    return 0;

}